export function NitroGlassContainer({ children }: React.PropsWithChildren) {
  return <>{children}</>;
}

export function NitroGlassView({ children }: React.PropsWithChildren) {
  return <>{children}</>;
}
